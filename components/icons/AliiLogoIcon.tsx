import React from 'react';

export const AliiLogoIcon: React.FC<{ className?: string }> = ({ className }) => (
    <div className={`w-10 h-10 bg-green-500 rounded-lg flex items-center justify-center font-sans font-bold text-white text-2xl ${className}`}>
        A
    </div>
);
